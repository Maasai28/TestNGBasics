package com.class06;public class Notes {
	
	/*TestNG - annotation based framework
	 * @Test - method annotated with @Test executed
	 * 
	 * Pre- condition - @Before Suite/Test/Class/Method
	 * postcondition -  @Before Suite/Test/Class/Method
	 * 
	 * Different attributes can be used wit annotation
	 *     .enable/disable -True/False - include/exclude using xml files
	 *     .priority
	 *     .groups
	 *     .dependancy
	 *     .alwaysRun
	 *     .innvocation count 
	 *     .dataProvider
	 *     
	 * xml- heart of TestNG
	 *    -specify all test settings
	 *    -we can execute multiple tests at the same time (batch testing)
	 *    
	 * Assertions 
	 *   - Hard - will stop execution if assertion fails
	 *   - Soft - will collect all failures - then we use assertAll to mark test case failed
	 *   
	 * Paratometers - to store data(small)
	 * 
	 * DataProvider - to perform data driven testing
	 * 
	 * TestNG provides many different default html reports
	 * 
	 */

}
